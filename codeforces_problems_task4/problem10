#include<iostream>
#include<cmath>
using namespace std;
int main()
{
	long double  a, b, c, d, num1, num2;
	cin >> a >> b >> c >> d;
	num1 =b*log(a);
	num2 =d *log(c );
	if (num1 > num2)
		cout << "YES" << endl;
	else if (num2 >= num1)
		cout << "NO" << endl;
	return 0;


}